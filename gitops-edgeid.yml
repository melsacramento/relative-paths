version: '3'
services:
 alpine-gitops:
  tty: true
  stdin_open: true
  image: alpine
  environment:
   - hello=hi
   - trigger=containerchange
  volumes:
   - ./html/${PORTAINER_EDGE_ID}.html:/home/config-edgeid
